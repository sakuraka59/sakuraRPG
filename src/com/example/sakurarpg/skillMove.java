package com.example.sakurarpg;

public class skillMove {
	//	[add angle, 1frame speed, frame num]

	float _add_angle = 0;
	float _speed = 0;
	float _frame_num = 0;
	float _check_b_2 = 0;

	public skillMove(float add_angle, float speed, float frame_num) {
		this._add_angle = add_angle;
		this._speed = speed;
		this._frame_num = frame_num;
	}
}
